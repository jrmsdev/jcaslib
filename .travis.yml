language: c

branches:
    only:
        - master

matrix:
    include:
        - os: linux
          sudo: false
          dist: trusty
          addons:
              apt:
                  packages:
                      - libgdbm-dev
                      - bmake
          compiler: clang
          before_script:
              - ./configure --use-gdbm
        - os: osx
          compiler: gcc
          install:
              - brew update --quiet
              - brew install bmake

script:
    - bmake depend
    - bmake -j2
    - bmake examples
    - bmake dist
    - ./build/lib/libjc.so
    - bmake check
