language: c

sudo: false

branches:
    only:
        - master

compiler: gcc

addons:
    apt:
        packages:
            - libgdbm-dev
            - pmake

script:
    - ./configure --use-gdbm
    - pmake
    - ./build/lib/libjc.so
