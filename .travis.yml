language: c

sudo: false

branches:
    only:
        - master

compiler: gcc

addons:
    apt:
        packages:
            - libgdbm-dev
            - bmake
            - pmake

script:
    - ./configure --use-gdbm
    - pmake || bmake
    - ./build/lib/libjc.so
