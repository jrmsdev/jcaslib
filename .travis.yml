language: c

sudo: false

branches:
    only:
        - master

compiler:
    - gcc
    - clang

matrix:
    include:
        - os: linux
          dist: trusty
          addons:
              apt:
                  packages:
                      - libgdbm-dev
                      - bmake
          env:
              - MAKE="bmake"
        - os: osx
          env:
              - MAKE="make"

before_script:
    - ./configure --use-gdbm
    - $MAKE depend
    - $MAKE -j2
    - $MAKE examples
    - $MAKE dist

script:
    - ./build/lib/libjc.so
    - $MAKE check
