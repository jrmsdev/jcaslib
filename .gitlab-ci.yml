image: gcc

build:
    stage: build
    before_script:
        - apt-get update && apt-get install -y bmake libgdbm-dev
        - cc --version
    script:
        - ./configure --use-gdbm
        - bmake
        - LD_LIBRARY_PATH=build/lib build/bin/jclib
        - bmake check
